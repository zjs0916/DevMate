# FastAPI uv project

## Description
Create a FastAPI project using uv and pyproject.toml.

## Procedure
Use pyproject.toml, avoid requirements.txt, use logging instead of print, and keep route handlers small.
